https://leetcode-cn.com/problems/stamping-the-grid/solution/wu-nao-zuo-fa-er-wei-qian-zhui-he-er-wei-zwiu/

```
class Solution:
    def possibleToStamp(self, grid: List[List[int]], stampHeight: int, stampWidth: int) -> bool:
        """
        有序序列+贪心
        1 将所有格子按「可能程度」放入有序序列
        2 从有序序列取最有可能的点，把将覆盖的点染色（从有序序列中去除）
        3 循环上面操作，如果循环的后，有序序列的长度不变，返回false
        4 最后返回true
        
        可能程度 满分是 stampHeight * stampWidth
        坏点(r,c)会破坏周围一圈的可能程度，周围一圈每个减一
        行范围是[r-stampHeight+1, r+stampHeight-1]，列范围是[c-stampWidth+1, c+stampWidth-1]
        但这一步的时间复杂度会达到O(n^2)
        """
        rows = len(grid)
        cols = len(grid[0])
        fen = stampHeight * stampWidth
        graph = [[fen for _ in range(cols)] for _ in range(rows)]
        
        for r in range(rows):
            for c in range(cols):
                if grid[r][c] == 1:
                    # 周围点减分
```

